import { TripfilterPipe } from './tripfilter.pipe';

describe('TripfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TripfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
